const Discord = require('discord.js'); exports.run = async(client, message, args) => { var user = message.mentions.members.first(); if(!user) return message.channel.send({embed: {color: "BLACK", description: "Bir kişi belirt!"}}); var isim = args.slice(1).join(" "); if(!isim || isim.length > 32) return message.channel.send({embed: {color: "BLACK", description: "Lütfen en fazla `32` hane olmak üzere bir isim belirtin."}}); user.setNickname(`${isim}`); return message.channel.send({embed: {color: "BLACK", description: "Belirtilen kişinin kullanıcı adı ayarlandı."}});}; exports.conf = {aliases: [], permLevel: 0}; exports.help = {name: "isim-değiştir"};